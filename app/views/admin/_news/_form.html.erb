<%= form.fields_for :content do |f| %>
  <%= f.text_field :title, :required => true %>
  <%= f.select :language, %w[ru zh en], :required => true %>
  <%= f.text_area :content, class: "tinymce" %>
<% end %>